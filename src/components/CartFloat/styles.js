import { makeStyles, fade } from '@material-ui/core/styles';

const drawerWidth = 0;

export default makeStyles((theme) => ({
  button: {
      position: 'fixed',
      top: '80px',
      right: '16px',
      zIndex: '100',
      background: '#3c40c6',
      borderRadius: '50%',
      color: '#ffdd59'
  }
}));